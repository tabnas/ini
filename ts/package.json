{
  "name": "@tabnas/ini",
  "version": "0.2.1",
  "description": "This plugin allows the [Tabnas](https://github.com/tabnas/jsonic) JSON parser to support INI syntax.",
  "main": "dist/ini.js",
  "type": "commonjs",
  "types": "dist/ini.d.ts",
  "homepage": "https://github.com/tabnas/ini",
  "keywords": [
    "json",
    "jsonic",
    "parser",
    "ini"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/ini.git",
    "directory": "ts"
  },
  "scripts": {
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "embed": "node embed-grammar.js",
    "watch": "tsc --build src test -w",
    "build": "node embed-grammar.js && tsc -p node_modules/@tabnas/hoover/src/tsconfig.json && tsc --build src test",
    "clean": "rm -rf dist dist-test node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "LICENSE"
  ],
  "peerDependencies": {
    "@tabnas/hoover": ">=0",
    "@tabnas/jsonic": ">=0",
    "@tabnas/parser": ">=0"
  },
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/hoover": "*",
    "@tabnas/jsonic": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=24"
  }
}
